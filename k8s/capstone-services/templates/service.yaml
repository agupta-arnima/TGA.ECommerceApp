apiVersion: v1
kind: Service
metadata:
  name: {{ include "capstone-services.fullname" . }}
  labels:
    {{- include "capstone-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capstone-services.selectorLabels" . | nindent 4 }}
