apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "capstone-services.fullname" . }}-test-connection"
  labels:
    {{- include "capstone-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "capstone-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
